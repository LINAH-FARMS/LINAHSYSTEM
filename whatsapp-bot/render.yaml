services:
  - type: web
    name: linahsystem-whatsapp-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: WHATSAPP_GROUP_NAMES
        value: صيانة
